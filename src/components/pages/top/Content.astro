---
export interface Props {
  title : string
  content : string
}

const { title, content } = Astro.props
---

<h2 class="l-section__heading l-section__heading--secondary">{title}</h2>
<p class="l-section__panel" >{content}</p>